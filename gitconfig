[user]
    email = thedch@protonmail.com
    name = Daniel H
[core]
    excludesfile = ~/.gitignore_global
[push]
    default = current
# [url "ssh://git@github.com/"]
    # insteadOf = https://github.com/
[grep]
	lineNumber = true
[pager]
	branch = false
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
